# Situation report — 2026-05-01

## Summary

Consolidated runtime constants into `settings.py` (removed `config.py`). Hardened the local conversion API to bind only on `127.0.0.1` and aligned the CLI (`slidedrop api`) with no configurable host. Added optional Windows Authenticode signing driven by GitHub Actions secrets and documented in the README.

## Files touched

- `src/slidedrop/settings.py`, removed `src/slidedrop/config.py`, import updates in discovery/scanner/ui.
- `src/slidedrop/services/local_api.py`, `src/slidedrop/cli.py`.
- `scripts/windows/sign-exe.ps1`, `scripts/windows/build.ps1`, `.github/workflows/release-windows.yml`.
- `README.md`, `CHANGELOG.md`.

## Verification

- `python -m pytest --cov=slidedrop --cov-fail-under=65` — pass (coverage ~76%).
